import { app_color_main } from "../services/data";

export const baseUri = 'https://kati.watch/mobile_api/';
export const baseSite = 'https://kati.watch/';
export const baseShared = 'https://kati.watch/watch-';

export const DRAWER_HEADER_SHOWN = false;
export const HEIGHT_HEADER = 60;

export const refresh_colors = ['black', 'orange', app_color_main]